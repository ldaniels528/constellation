package constellation;

/**
 * Represents the enumeration of Select Mode
 * @author lawrence.daniels@gmail.com
 */
public enum SelectionMode {
	
	/**
	 * Represents all elements currently in the model
	 */
	PHYSICAL_ELEMENTS, 
	
	/**
	 * Represents all phantom (deleted) elements in the model
	 */
	PHANTOM_ELEMENTS
}
